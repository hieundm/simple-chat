import React from "react";

const FriendList = () => {
	const [friends, setFriends] = React.useState(null);

	const init = async () => {};

	return (
		<React.Fragment>
			<div className="messenger__friend-list"></div>
		</React.Fragment>
	);
};

export default FriendList;
